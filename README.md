# devclean

Development environment cleanup CLI (scaffold).

This is an initial, minimal Rust CLI skeleton. Product behavior is not yet
implemented; the binary currently only parses its arguments and prints a
placeholder banner.

## Build

```sh
cargo build
```

## Run

```sh
cargo run -- --version
```

## Test

```sh
cargo test
```
